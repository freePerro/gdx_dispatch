# Routes reachable WITHOUT an authentication dependency — frozen baseline.
#
# Generated by gdx_dispatch/tests/authz_sweep.py and enforced by
# gdx_dispatch/tests/test_authz_route_sweep.py. This is a RATCHET, not an
# approval list: every line here was already open on 2026-08-04, and CI now
# fails when a NEW ungated route appears. Adding a line must be a deliberate,
# reviewed act.
#
# Being listed does NOT mean "safe". These fall into three groups:
#   1. Public by design — /health, /metrics, /manifest.json, /.well-known/*,
#      the login endpoints, and signature-verified webhooks (Stripe, Twilio,
#      QuickBooks) which authenticate by verifying the signature instead.
#   2. Authorized by an unguessable token rather than a session — the
#      /pay/{token} page and the /api/payments/* endpoints behind it, plus
#      /api/signatures/token/{token}. For these the token IS the credential;
#      see core/payments.py "Public-payment authorization".
#   3. NOT yet reviewed — the remainder. This is the worklist. Several look
#      like staff surfaces that should require a session (parts adjustment,
#      push send, job parts). Work them down; do not add to them.
#
# Regenerate after deliberately changing the surface:
#   python -c "from gdx_dispatch.tests.authz_sweep import ungated_routes; \
#              open('.authz_ungated_baseline','a').write('')"
# ...then paste the new list below this header.
DELETE /api/push/unsubscribe
GET /.well-known/gdx-platform
GET /.well-known/mcp-tools
GET /.well-known/oauth-authorization-server
GET /.well-known/oauth-protected-resource
GET /.well-known/oauth-protected-resource/{resource_path:path}
GET /.well-known/openid-configuration
GET /.well-known/security.txt
GET /api/.well-known/jwks.json
GET /api/ai/pricing/health
GET /api/ai/usage
GET /api/ai/usage/export
GET /api/bank-feeds/oauth/callback
GET /api/diagnosis/schemas
GET /api/integrations/available
GET /api/integrations/zapier/triggers
GET /api/inventory/parts/{part_id}/stock
GET /api/meta/deprecations
GET /api/meta/versions
GET /api/oauth/outlook/callback
GET /api/onboarding
GET /api/proposals/{token}
GET /api/qb/callback
GET /api/qb/oauth/callback
GET /api/recommendations
GET /api/settings/branding/logo/{filename}
GET /api/signatures/token/{token}
GET /api/supplier/portal/catalog
GET /api/supplier/portal/delivery-loadsheet
GET /api/supplier/portal/orders
GET /api/surveys/public/{token}
GET /api/webhooks/outlook/{tenant_slug}/{client_state}
GET /auth/sso/google
GET /auth/sso/google/callback
GET /auth/sso/microsoft
GET /auth/sso/microsoft/callback
GET /health
GET /integrations
GET /legacy/billing
GET /legacy/dashboard
GET /legacy/settings
GET /legacy/team
GET /manifest.json
GET /metrics
GET /pay/{invoice_token}
GET /portal/verify
GET /public/door-listings.json
GET /public/door-listings/{photo_id}.jpg
GET /pwa/manifest.json
GET /pwa/version
GET /superadmin
GET /supplier/join/{token}
GET /supplier/join/{token}/form.js
GET /sw.js
GET /{full_path:path}
PATCH /api/supplier/portal/orders/{order_id}
POST /api/ai/pricing/suggest
POST /api/ai/quote
POST /api/feedback/client-error
POST /api/inventory/parts/{part_id}/adjust
POST /api/jobs/{job_id}/parts
POST /api/payments/ach/charge
POST /api/payments/ach/setup
POST /api/payments/confirm
POST /api/payments/create-intent
# Group 2 (token IS the credential): the public estimate approval page.
# 64-char urlsafe Estimate.public_token, sent_at+deleted_at gated, uniform
# 404, row-locked accept — see modules/proposals/router.py "public customer
# surface" (2026-08-13, same model as GET /api/proposals/{token} above).
POST /api/proposals/{token}/accept
POST /api/proposals/{token}/decline
# Same Group-2 model (2026-08-18): mints the deposit invoice at the moment
# the customer moves to pay online — token-resolved estimate, accepted-only
# (409 otherwise), row-locked (for_update), idempotent via
# find_deposit_invoice_for_estimate; amount is SERVER-derived from the
# tenant deposit percent (deposit_ask_for), never caller input.
POST /api/proposals/{token}/deposit/pay
POST /api/push/send
POST /api/push/send-user/{user_id}
POST /api/push/subscribe
POST /api/qb/webhooks
POST /api/recommendations/{rec_type}/dismiss
POST /api/signatures/token/{token}
POST /api/stripe/connect/webhook
POST /api/supplier/login
POST /api/supplier/portal/catalog
POST /api/supplier/register
POST /api/surveys/public/{token}
POST /api/webhooks/outlook/{tenant_slug}/{client_state}
POST /api/webhooks/phone-com/{tenant_slug}/{secret}
POST /auth/forgot-password
POST /auth/login
POST /auth/logout
POST /auth/refresh
POST /auth/reset-password
POST /legacy/settings
POST /legacy/team/invite
POST /payments/connect/webhook
POST /portal/login
POST /portal/login/password
POST /stripe/webhook
PUT /api/inventory/parts/{part_id}
