# Generated by gdx_dispatch/tools/route_shadow_scan.py --write-baseline
# One METHOD path per line. Goal: drive to zero via
# ai-queue/plans/sprint_three_plane_cleanup.md S2/S3.
#
# ZERO, reached 2026-09-12. The count was 43 when #462 was filed; the
# dead-duplicates work (#569, #633, #642, #655) took it to 1, and removing the
# shadowed `POST /api/customers/bulk-tag` twin from ui_compat took it to 0.
# `POST /api/jobs/{job_id}/line-items` lost its twin earlier, in #653.
#
# Deliberately left empty rather than deleted: `load_baseline()` returns an
# empty set for a missing file, so the gate would still pass — but silently,
# and an empty exemption list that exists is a stronger statement than one
# that does not.
