{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdx.job.completed.v1",
  "description": "Emitted when a job transitions to completed status.",
  "type": "object",
  "required": ["job_id", "tenant_id", "completed_at", "completed_by_identity_id"],
  "properties": {
    "job_id": {"type": "string"},
    "tenant_id": {"type": "string"},
    "completed_at": {"type": "string"},
    "completed_by_identity_id": {"type": "string"},
    "total_amount_cents": {"type": "integer"},
    "custom_fields": {"type": "object", "additionalProperties": true}
  },
  "additionalProperties": true
}
