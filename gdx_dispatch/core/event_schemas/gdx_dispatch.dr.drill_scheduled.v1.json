{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdx.dr.drill_scheduled.v1",
  "description": "Emitted when a DR drill is scheduled (ahead of execution). Carries drill_run_id + scheduled_for; the matching drill_started.v1 fires when the orchestrator actually begins the pipeline.",
  "type": "object",
  "required": ["drill_run_id", "scheduled_for", "scope"],
  "properties": {
    "drill_run_id": {"type": "string"},
    "scheduled_for": {"type": "string"},
    "scope": {"type": "string", "enum": ["full", "tenant", "schema"]},
    "scope_selector": {"type": ["string", "null"]},
    "scheduled_by_identity_id": {"type": ["string", "null"]},
    "notes": {"type": ["string", "null"]}
  },
  "additionalProperties": false
}
