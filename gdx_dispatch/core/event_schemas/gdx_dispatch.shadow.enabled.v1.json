{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "gdx.shadow.enabled.v1",
  "description": "Emitted when a (tenant, old_table) pair transitions from off to shadow mode.",
  "type": "object",
  "required": ["tenant_id", "old_table", "new_table", "enabled_at"],
  "properties": {
    "tenant_id": {"type": "string"},
    "old_table": {"type": "string"},
    "new_table": {"type": "string"},
    "enabled_at": {"type": "string"},
    "actor_identity_id": {"type": ["string", "null"]},
    "notes": {"type": ["string", "null"]}
  },
  "additionalProperties": false
}
