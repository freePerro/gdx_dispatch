"""Platform integration tests — SS-5 Slice A foundation.

Each module in this package proves a specific cross-cutting behavior of the
platform against the full SS-2/SS-3 schema in an isolated in-memory DB.
Scope is "scaffold" for Slice A — later slices deepen coverage.
"""
