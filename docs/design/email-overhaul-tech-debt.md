# Tech debt log — email overhaul build (feat/email-overhaul)

**Status:** **DEBT REGISTER — not a plan.** Nothing here is scheduled; each row
records debt found (not created) during the email overhaul, with the reason it
was deferred. Do not read an unchecked row as unfinished work on that build.

Debt found (not created) while building docs/design/email-readability-and-delivery-plan.md.
Each entry: what it is, where, why it wasn't fixed in this branch.

| # | Found | Where | Debt | Why deferred |
|---|-------|-------|------|--------------|
| 1 | 2026-08-18 | routers/communications.py | Outbound email/thread store is a process-local in-memory dict (`_EMAILS_BY_TENANT`) wiped on restart; endpoints look persistent but aren't. The new `outbound_emails` table supersedes it for transactional mail, but the communications screens still read the dict. | Migrating those screens to the new table is a UI project of its own; out of scope here. **Resolved 2026-08-31 by deletion** — screens, router and dict removed (#350, communications-parallel-fake-removal-plan.md). |
| 2 | 2026-08-18 | routers/supplier_invite.py | Docstring claims "supplier gets email with link"; no email is ever sent. | Needs a product decision on whether supplier invites should email at all. |
| 3 | 2026-08-18 | routers/communications.py + core/email.py | The communications email path injects the legacy SES-style `core/email.py` sender and stores messages in the in-memory dict — separate from the transactional pipeline and the outbound_emails log. | Migrating the communications screens onto the audited pipeline is the same UI project as row 1. **Resolved 2026-08-31 with row 1** — `core/email.py` deleted alongside its only importer (#350). |
| 4 | 2026-08-18 | modules/workflows/engine.py | send_sms / create_followup_task / emit_webhook / update_job_field actions now honestly report `not_implemented` (previously "logged"). Executors still to build. | Out of email-overhaul scope; the send_email executor structure makes each mechanical. |
| 5 | 2026-08-18 | routers/supplier_invite.py | Invite mints a link but sends no email; docstring now says so. A real invite email through the pipeline is unbuilt. | Needs a product decision (who sends supplier mail, which template). |
| 6 | 2026-08-18 | routers/invoices.py | Invoice + receipt email subject/body templates remain hardcoded consts (estimate templates are tenant-editable end-to-end). Composer override covers per-send edits. | The locked decision covered ESTIMATE templates; making invoice/receipt tenant-editable = settings columns + UI, deferred to keep this branch reviewable. |
| 6 | **2026-08-31** | (same) | **DONE — this PR (issue #351).** Migration `086_invoice_receipt_templates` adds `invoice_email_subject_template` / `invoice_email_body_template` / `receipt_email_subject_template` / `receipt_email_body_template` (nullable text) to `tenant_settings`; `modules/estimates_features` reads them (`EstimatesFeatures`) and round-trips them through `GET/PATCH /api/estimates-features` (the PATCH now writes an `estimates_features_updated` audit row — it never had one); `routers/invoices._invoice_email_templates` resolves tenant copy over the unchanged default consts for compose, preview, one-click/bulk send and send-receipt alike; Settings → Estimates carries the four fields with the full `ctx` placeholder list and the "keep `Invoice {{invoice_number}} from` in the subject" bounce note. Blank = platform default, nothing backfilled, so no install's outgoing copy changes on upgrade. | Closed. Scope: the office invoice send, the office paid-invoice receipt AND the truck's invoice send (routed through `_prepare_invoice_email` — audit catch); the truck's per-payment receipt keeps its own fixed copy (no payment placeholders exist). |
| 7 | 2026-08-18 | bounce_detect.py | Subject-serial (rung 1) matching depends on templates containing the serial; the default estimate subject ({{job_title}}) never matched it — rung 2/3 + the new outbound_emails kind check carry detection. A durable message-id correlation (Graph internetMessageHeaders) would be stronger. | Needs NDR-side header propagation research. |
| 8 | 2026-08-18 | core/transactional_email.py recently_sent | Double-send guard is check-then-act over committed rows — a same-instant race (two requests in flight simultaneously) passes both; only the sequential re-click class is stopped. | A true constraint needs a dedicated idempotency table/key; not worth it for the observed failure mode. Documented in the docstring. |
| 9 | 2026-08-18 | modules/outlook/send_router.py | Ad-hoc /api/outlook/send (inbox replies, JobDetailView's generic composer) writes no outbound_emails row — "every send audited" covers the transactional surfaces only. | Those sends live in the rep's own mailbox (Sent Items) like normal correspondence; auditing them means intercepting all mailbox use — a product decision. |
| 10 | 2026-08-18 | routers/automations.py + modules/outlook/automations.py | Two MORE automation systems besides modules/workflows: routers/automations (sequences, still decorative) and outlook/automations.dispatch_trigger (zero callers, would bypass the audit trail if wired). Consolidation needed. | Out of scope; the workflows engine is now the one real path. |
| 10 | **2026-08-31** | (same) | **RETIRED — both removed.** `routers/automations.py` (+ its page, nav entry, 14 tests) and `modules/outlook/automations.py` (+ its tests) are deleted; `/automations` redirects to `/automation-rules`. Tables kept, no DDL. See unimplemented-endpoints-decision-list.md § 2026-08-31. | Done in the retirement PR; `modules/workflows` is the only automation path left in the tree. |
| 11 | 2026-08-18 | openapi.json (repo root) | Checked-in spec remains broadly stale; only frontend/src/types/api.d.ts was regenerated (from a live app spec) on this branch. The invariants registry's claimed drift gate tool doesn't exist. | A wholesale openapi.json refresh is a giant unrelated diff; needs its own PR + an actual gate script. |
| 11 | **2026-08-31** | (same) | **DONE in its own PR.** `gdx_dispatch/tools/openapi_snapshot.py` (`--write` / `--check`) pins the route table in `gdx_dispatch/openapi_routes.txt` (the stale 2.8 MB `openapi.json` — 53 operations gone, 48 missing, and not even deterministic — is deleted; `/openapi.json` is live) and `tests/test_openapi_snapshot_current.py` gates it in the default suite, plus router importability and cross-process determinism. `api.d.ts` had no importer — deleted with `openapi-typescript`. Invariants row 4 rewritten and marked enforced. | Closed. |
| 12 | 2026-08-18 | Event Rules screen | Conditions builder not implemented (rules run on every matching event); only the send_email action is offered (others are not_implemented in the engine). | v1 keeps the screen honest — it offers exactly what works. |
